# External data directory

Large public resources are not committed. Follow `docs/DATA_PROVENANCE.md` and
place downloaded files under this directory. The model entry points also accept
explicit command-line paths, so symbolic links are acceptable.
